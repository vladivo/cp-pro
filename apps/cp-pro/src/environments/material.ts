import { MatFormFieldDefaultOptions } from '@angular/material/form-field';
import { MatIconDefaultOptions } from '@angular/material/icon';

export const FormFieldOptions: MatFormFieldDefaultOptions = {
  appearance: 'standard',
};

export const IconOptions: MatIconDefaultOptions = {
  color: 'primary',
};
